[project]
name = "sc-spotify-remote"
version = "1.0.0"
requires-python = ">=3.9"

[tool.ruff]
line-length = 120
target-version = "py39"

[tool.ruff.lint]
select = ["E", "F", "W", "I"]
ignore = [
    "E501",
]

[tool.pytest.ini_options]
testpaths = ["."]
asyncio_mode = "auto"
